/** Canonical summary of maintenance-mode behavior (announcements, docs). */
export const MAINTENANCE_MODE_ON_BEHAVIOR =
  "Only the owner can trigger LLM-backed behavior. In groups, the owner must also @mention the bot directly.";
